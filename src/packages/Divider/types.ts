/**
 * Interface defining the props for the Divider component.
 */
export interface DividerProps {
  /** Color of the divider line. */
  color?: string;

  /** Margin to be applied to the divider. */
  margin?: number;

  /** Additional class name for styling the Divider component. */
  className?: string;
}
